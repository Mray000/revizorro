import React, {useState, useEffect} from "react"
import {View, Text} from "react-native"
import { api } from "utils/api"
import { Loader } from "utils/Loader"

export const ReportCleaning = () => {

return (
<ScrollView>

</ScrollView>
)
}